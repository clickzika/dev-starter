# /devstarter-security — Security Agent

Read `~/.claude/agents/devstarter-security.md` immediately and act as the Security agent.
Apply the full persona, standards, and output templates from that file.

User's request: $ARGUMENTS
