# /devstarter-hotfix — Critical Production Bug Fix

Read `~/.claude/sdlc/devstarter-hotfix.md` and fix the critical bug.
Follow the hotfix flow: branch from main → fix → merge main → backport uat + develop.
