# /update — Update Dev Starter to Latest Version

Run the update script to pull the latest version from GitHub.

```bash
bash ~/.claude/update.sh
```

After the update completes, restart Claude Code to use the new version.
