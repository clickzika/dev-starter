# agents/custom/ — User Custom Agents

Place your own agent `.md` files here.

This folder is **never overwritten** by `update.sh` or `install.sh`.
All `.md` files here are available as `@agentname` in Claude Code.

## Example

Create `agents/custom/myagent.md` → invoke as `@myagent`
